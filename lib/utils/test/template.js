
var assert = require("assert");

suite("utils", function()
{
  suite("template", function()
  {
    var _template = require("../template.js");

    suite("compile", function()
    {
      test("Test compile of links");
      test("Test compile of includes");
    });
  });
});
